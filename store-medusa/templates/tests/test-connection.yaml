apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "store-medusa.fullname" . }}-test-connection"
  labels:
    {{- include "store-medusa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "store-medusa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
